<h1>Mis paradas </h1>
<p><%= link_to 'Agregar otra parada', locations_path %></p>
<p>Your permanent url is <%= link_to "http://misparadas.com/#{session[:choices_slug]}" %>
</p><% @choices.each do |choice| %>
  <%= render :partial => 'locations/location_with_details', :locals => { :location => choice.location, :choice => choice } %>
<% end %>

