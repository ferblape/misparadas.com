
%section.choices.index
  %p.linkInfo
    Agrega este enlace a tus favoritos para consultarlo después
    %br
    = link_to slug_url(:slug => session[:slug])

  - @choices.each do |choice|
    = render :partial => 'locations/location_with_details', :locals => { :location => choice.location, :choice => choice }
    %p
      = link_to 'Agregar otra parada', locations_path
